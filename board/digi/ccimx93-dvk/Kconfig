if TARGET_CCIMX93_DVK

config SYS_BOARD
	default "ccimx93-dvk"

config SYS_VENDOR
	default "digi"

config SYS_CONFIG_NAME
	default "ccimx93-dvk"

config CCIMX93_DVK_LPDDR4X
	bool "Using LPDDR4X Timing and PMIC voltage"
	default y
	select IMX9_LPDDR4X
	help
	  Select the LPDDR4X timing and 0.6V VDDQ

source "board/digi/common/Kconfig"

endif
